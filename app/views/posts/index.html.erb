<h1>Blog</h1>

<div id="posts">
	<% @posts.each do |post| %>
		<h3><%= post.title %></h3>
		<p>
			<%= post.content %>
		</p>
		<p>
			<strong>Created at: <%= post.created_at %></strong>
		</p>
	<% end %>
</p>
